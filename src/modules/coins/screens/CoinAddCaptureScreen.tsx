// Pantalla legacy — reemplazada por CoinAddCoinScreen. Se conserva para no romper imports externos.
import React from 'react';
import { View } from 'react-native';
export const CoinAddCaptureScreen: React.FC = () => <View />;
