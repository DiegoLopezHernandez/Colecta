package com.colecta.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Paleta de Colecta. Tema oscuro con acento dorado numismático.
 * Coincide con los tokens del proyecto original para mantener la identidad visual.
 */
object ColectaColors {
    // Fondos
    val Bg = Color(0xFF0B0B0D)
    val Surface = Color(0xFF141417)
    val Surface2 = Color(0xFF1C1C20)
    val Surface3 = Color(0xFF26262B)

    // Bordes
    val Border = Color(0xFF26262B)
    val BorderStrong = Color(0xFF3A3A40)
    val BorderSubtle = Color(0xFF1C1C20)

    // Texto
    val OnBg = Color(0xFFF4F4F5)
    val OnBgMuted = Color(0xFFA1A1AA)
    val OnBgSubtle = Color(0xFF71717A)
    val OnBgDim = Color(0xFF52525B)

    // Acento dorado
    val Primary = Color(0xFFD4A24B)
    val PrimaryDim = Color(0xFFA87E2E)
    val OnPrimary = Color(0xFF1A1206)

    // Semánticos
    val Ok = Color(0xFF4ADE80)
    val Warn = Color(0xFFFBBF24)
    val Err = Color(0xFFF87171)
    val OnErr = Color(0xFF1A0606)

    // Misc
    val Rarity = Color(0xFFC084FC)
    val Overlay = Color(0xA6000000)
}
